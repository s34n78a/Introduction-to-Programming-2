#include <stdio.h> // Include the standard input/output library

int main(void) { // Main function - entry point of the program, void indicates it takes no arguments, int indicates it returns an integer
    printf("Hello, World!\n");
    return 0;
}